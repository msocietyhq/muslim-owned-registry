# Copy to .env.local. Never commit real keys.

DATABASE_URL=
DATABASE_URL_UNPOOLED=
BETTER_AUTH_SECRET=
NEON_PROJECT_ID=little-scene-49442607

NEXT_PUBLIC_SITE_URL=http://localhost:3000

# First admin. Comma-separated.
ADMIN_EMAILS=afiq980@gmail.com

# Dev only: return the OTP in the API response so you can log in without Resend.
ALLOW_DEV_OTP=true

# Optional until you rotate the leaked keys and are ready to send real mail.
RESEND_API_KEY=
MAIL_FROM="muslimowned.sg <contact@playtours.email>"

# Optional until the planner is used.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat

# Daily confirmation job (Railway Cron). Required in production.
JOBS_SECRET=
